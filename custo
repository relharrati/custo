#!/usr/bin/env python3
"""Custo CLI Entry Point"""

import sys
from pathlib import Path

# Add the project root to Python path
root = Path(__file__).parent
sys.path.insert(0, str(root))

from interfaces.terminal.cli import main

if __name__ == "__main__":
    main()
